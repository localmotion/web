<%= simple_form_for(@sign_up, html: { class: 'pure-form' }) do |f| %>
  <fieldset>
    <%= f.text_field :email, placeholder: 'Your email' %>
    <%= f.submit 'Sign Up!', class: 'brbn-btn' %>
  </fieldset>
<% end %>